spring:
  application:
    name: ms-notification
  datasource:
    driverClassName: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/db_notification?createDatabaseIfNotExist=true&serverTimezone=UTC
    username: root
    password: 1234567
  jpa:
    hibernate:
      ddl-auto: update
    generate-ddl: true
    show-sql: true
    open-in-view: true
  
  rabbitmq:
    host: localhost
    port: 5672
    username: guest
    password: guest

mq:
  queue:
    event-notification: event-notification 